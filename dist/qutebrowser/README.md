# Subway Seat for qutebrowser

Sets every `c.colors.*` option: tabs, status bar modes, completion, hints, prompts, messages and downloads, plus the page background and preferred color scheme so new tabs don't flash white.

[qutebrowser](https://qutebrowser.org) · [Previews and copy buttons](https://oddurs.github.io/subway-seat/ports/qutebrowser/)

## Files

| Flavor | File | Where it goes |
|---|---|---|
| Subway Seat | [`subway_seat.py`](subway_seat.py) | `~/.config/qutebrowser/subway_seat.py`; next to config.py; on macOS that's ~/.qutebrowser/ |
| Subway Seat Tunnel | [`subway_seat_tunnel.py`](subway_seat_tunnel.py) | `~/.config/qutebrowser/subway_seat_tunnel.py`; next to config.py; on macOS that's ~/.qutebrowser/ |
| Subway Seat Enamel | [`subway_seat_enamel.py`](subway_seat_enamel.py) | `~/.config/qutebrowser/subway_seat_enamel.py`; next to config.py; on macOS that's ~/.qutebrowser/ |
| London Moquette | [`london_moquette.py`](london_moquette.py) | `~/.config/qutebrowser/london_moquette.py`; next to config.py; on macOS that's ~/.qutebrowser/ |
| London Deep Level | [`london_deep_level.py`](london_deep_level.py) | `~/.config/qutebrowser/london_deep_level.py`; next to config.py; on macOS that's ~/.qutebrowser/ |
| London Portland | [`london_portland.py`](london_portland.py) | `~/.config/qutebrowser/london_portland.py`; next to config.py; on macOS that's ~/.qutebrowser/ |
| Paris Guimard | [`paris_guimard.py`](paris_guimard.py) | `~/.config/qutebrowser/paris_guimard.py`; next to config.py; on macOS that's ~/.qutebrowser/ |
| Paris Catacombes | [`paris_catacombes.py`](paris_catacombes.py) | `~/.config/qutebrowser/paris_catacombes.py`; next to config.py; on macOS that's ~/.qutebrowser/ |
| Paris Carrelage | [`paris_carrelage.py`](paris_carrelage.py) | `~/.config/qutebrowser/paris_carrelage.py`; next to config.py; on macOS that's ~/.qutebrowser/ |

## Turn it on

**Subway Seat**, in ~/.config/qutebrowser/config.py (macOS: ~/.qutebrowser/config.py):

```python
config.load_autoconfig()
config.source("subway_seat.py")
```

**Subway Seat Tunnel**, in ~/.config/qutebrowser/config.py (macOS: ~/.qutebrowser/config.py):

```python
config.load_autoconfig()
config.source("subway_seat_tunnel.py")
```

**Subway Seat Enamel**, in ~/.config/qutebrowser/config.py (macOS: ~/.qutebrowser/config.py):

```python
config.load_autoconfig()
config.source("subway_seat_enamel.py")
```

**London Moquette**, in ~/.config/qutebrowser/config.py (macOS: ~/.qutebrowser/config.py):

```python
config.load_autoconfig()
config.source("london_moquette.py")
```

**London Deep Level**, in ~/.config/qutebrowser/config.py (macOS: ~/.qutebrowser/config.py):

```python
config.load_autoconfig()
config.source("london_deep_level.py")
```

**London Portland**, in ~/.config/qutebrowser/config.py (macOS: ~/.qutebrowser/config.py):

```python
config.load_autoconfig()
config.source("london_portland.py")
```

**Paris Guimard**, in ~/.config/qutebrowser/config.py (macOS: ~/.qutebrowser/config.py):

```python
config.load_autoconfig()
config.source("paris_guimard.py")
```

**Paris Catacombes**, in ~/.config/qutebrowser/config.py (macOS: ~/.qutebrowser/config.py):

```python
config.load_autoconfig()
config.source("paris_catacombes.py")
```

**Paris Carrelage**, in ~/.config/qutebrowser/config.py (macOS: ~/.qutebrowser/config.py):

```python
config.load_autoconfig()
config.source("paris_carrelage.py")
```

## Uninstall

- Delete `~/.config/qutebrowser/subway_seat.py`.
- Delete `~/.config/qutebrowser/subway_seat_tunnel.py`.
- Delete `~/.config/qutebrowser/subway_seat_enamel.py`.
- Delete `~/.config/qutebrowser/london_moquette.py`.
- Delete `~/.config/qutebrowser/london_deep_level.py`.
- Delete `~/.config/qutebrowser/london_portland.py`.
- Delete `~/.config/qutebrowser/paris_guimard.py`.
- Delete `~/.config/qutebrowser/paris_catacombes.py`.
- Delete `~/.config/qutebrowser/paris_carrelage.py`.
- Remove the line you added to turn it on.

Generated by `build.py` from `palette.py` (v0.3.0). Edit the port in `ports/`, not these files.
