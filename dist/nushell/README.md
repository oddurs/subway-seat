# Subway Seat for Nushell

The fish port's command-line colors as a `color_config`, plus table output where file sizes and ages run down the 70s stripe, and the `explore` pager. It turns on `highlight_resolved_externals`, so unknown commands show red as they do in fish.

[Nushell](https://www.nushell.sh) · [Previews and copy buttons](https://oddurs.github.io/subway-seat/ports/nushell/)

## Files

| Flavor | File | Where it goes |
|---|---|---|
| Subway Seat | [`subway-seat.nu`](subway-seat.nu) | `~/.config/nushell/themes/subway-seat.nu` |
| Subway Seat Tunnel | [`subway-seat-tunnel.nu`](subway-seat-tunnel.nu) | `~/.config/nushell/themes/subway-seat-tunnel.nu` |
| Subway Seat Enamel | [`subway-seat-enamel.nu`](subway-seat-enamel.nu) | `~/.config/nushell/themes/subway-seat-enamel.nu` |
| London Moquette | [`london-moquette.nu`](london-moquette.nu) | `~/.config/nushell/themes/london-moquette.nu` |
| London Deep Level | [`london-deep-level.nu`](london-deep-level.nu) | `~/.config/nushell/themes/london-deep-level.nu` |
| London Portland | [`london-portland.nu`](london-portland.nu) | `~/.config/nushell/themes/london-portland.nu` |
| Paris Guimard | [`paris-guimard.nu`](paris-guimard.nu) | `~/.config/nushell/themes/paris-guimard.nu` |
| Paris Catacombes | [`paris-catacombes.nu`](paris-catacombes.nu) | `~/.config/nushell/themes/paris-catacombes.nu` |
| Paris Carrelage | [`paris-carrelage.nu`](paris-carrelage.nu) | `~/.config/nushell/themes/paris-carrelage.nu` |

## Turn it on

**Subway Seat**, in config.nu (`$nu.config-path`):

```nushell
source ~/.config/nushell/themes/subway-seat.nu
```

**Subway Seat Tunnel**, in config.nu (`$nu.config-path`):

```nushell
source ~/.config/nushell/themes/subway-seat-tunnel.nu
```

**Subway Seat Enamel**, in config.nu (`$nu.config-path`):

```nushell
source ~/.config/nushell/themes/subway-seat-enamel.nu
```

**London Moquette**, in config.nu (`$nu.config-path`):

```nushell
source ~/.config/nushell/themes/london-moquette.nu
```

**London Deep Level**, in config.nu (`$nu.config-path`):

```nushell
source ~/.config/nushell/themes/london-deep-level.nu
```

**London Portland**, in config.nu (`$nu.config-path`):

```nushell
source ~/.config/nushell/themes/london-portland.nu
```

**Paris Guimard**, in config.nu (`$nu.config-path`):

```nushell
source ~/.config/nushell/themes/paris-guimard.nu
```

**Paris Catacombes**, in config.nu (`$nu.config-path`):

```nushell
source ~/.config/nushell/themes/paris-catacombes.nu
```

**Paris Carrelage**, in config.nu (`$nu.config-path`):

```nushell
source ~/.config/nushell/themes/paris-carrelage.nu
```

## Uninstall

- Delete `~/.config/nushell/themes/subway-seat.nu`.
- Delete `~/.config/nushell/themes/subway-seat-tunnel.nu`.
- Delete `~/.config/nushell/themes/subway-seat-enamel.nu`.
- Delete `~/.config/nushell/themes/london-moquette.nu`.
- Delete `~/.config/nushell/themes/london-deep-level.nu`.
- Delete `~/.config/nushell/themes/london-portland.nu`.
- Delete `~/.config/nushell/themes/paris-guimard.nu`.
- Delete `~/.config/nushell/themes/paris-catacombes.nu`.
- Delete `~/.config/nushell/themes/paris-carrelage.nu`.
- Remove the line you added to turn it on.

Generated by `build.py` from `palette.py` (v0.3.0). Edit the port in `ports/`, not these files.
