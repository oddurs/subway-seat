# Subway Seat for vivid

LS_COLORS for GNU ls (macOS's own ls ignores it), fd, eza and friends: gold directories instead of blue, avocado executables, sage symlinks, terracotta media and red archives.

[vivid](https://github.com/sharkdp/vivid) · [Previews and copy buttons](https://oddurs.github.io/subway-seat/ports/vivid/)

## Files

| Flavor | File | Where it goes |
|---|---|---|
| Subway Seat | [`subway-seat.yml`](subway-seat.yml) | `~/.config/vivid/themes/subway-seat.yml` |
| Subway Seat Tunnel | [`subway-seat-tunnel.yml`](subway-seat-tunnel.yml) | `~/.config/vivid/themes/subway-seat-tunnel.yml` |
| Subway Seat Enamel | [`subway-seat-enamel.yml`](subway-seat-enamel.yml) | `~/.config/vivid/themes/subway-seat-enamel.yml` |
| London Moquette | [`london-moquette.yml`](london-moquette.yml) | `~/.config/vivid/themes/london-moquette.yml` |
| London Deep Level | [`london-deep-level.yml`](london-deep-level.yml) | `~/.config/vivid/themes/london-deep-level.yml` |
| London Portland | [`london-portland.yml`](london-portland.yml) | `~/.config/vivid/themes/london-portland.yml` |
| Paris Guimard | [`paris-guimard.yml`](paris-guimard.yml) | `~/.config/vivid/themes/paris-guimard.yml` |
| Paris Catacombes | [`paris-catacombes.yml`](paris-catacombes.yml) | `~/.config/vivid/themes/paris-catacombes.yml` |
| Paris Carrelage | [`paris-carrelage.yml`](paris-carrelage.yml) | `~/.config/vivid/themes/paris-carrelage.yml` |

## Turn it on

**Subway Seat**, in config.fish (bash and zsh: ~/.bashrc or ~/.zshrc), after copying the theme to ~/.config/vivid/themes/:

```fish
set -gx LS_COLORS (vivid generate subway-seat)
```

In bash or zsh:

```sh
export LS_COLORS="$(vivid generate subway-seat)"
```

**Subway Seat Tunnel**, in config.fish (bash and zsh: ~/.bashrc or ~/.zshrc), after copying the theme to ~/.config/vivid/themes/:

```fish
set -gx LS_COLORS (vivid generate subway-seat-tunnel)
```

In bash or zsh:

```sh
export LS_COLORS="$(vivid generate subway-seat-tunnel)"
```

**Subway Seat Enamel**, in config.fish (bash and zsh: ~/.bashrc or ~/.zshrc), after copying the theme to ~/.config/vivid/themes/:

```fish
set -gx LS_COLORS (vivid generate subway-seat-enamel)
```

In bash or zsh:

```sh
export LS_COLORS="$(vivid generate subway-seat-enamel)"
```

**London Moquette**, in config.fish (bash and zsh: ~/.bashrc or ~/.zshrc), after copying the theme to ~/.config/vivid/themes/:

```fish
set -gx LS_COLORS (vivid generate london-moquette)
```

In bash or zsh:

```sh
export LS_COLORS="$(vivid generate london-moquette)"
```

**London Deep Level**, in config.fish (bash and zsh: ~/.bashrc or ~/.zshrc), after copying the theme to ~/.config/vivid/themes/:

```fish
set -gx LS_COLORS (vivid generate london-deep-level)
```

In bash or zsh:

```sh
export LS_COLORS="$(vivid generate london-deep-level)"
```

**London Portland**, in config.fish (bash and zsh: ~/.bashrc or ~/.zshrc), after copying the theme to ~/.config/vivid/themes/:

```fish
set -gx LS_COLORS (vivid generate london-portland)
```

In bash or zsh:

```sh
export LS_COLORS="$(vivid generate london-portland)"
```

**Paris Guimard**, in config.fish (bash and zsh: ~/.bashrc or ~/.zshrc), after copying the theme to ~/.config/vivid/themes/:

```fish
set -gx LS_COLORS (vivid generate paris-guimard)
```

In bash or zsh:

```sh
export LS_COLORS="$(vivid generate paris-guimard)"
```

**Paris Catacombes**, in config.fish (bash and zsh: ~/.bashrc or ~/.zshrc), after copying the theme to ~/.config/vivid/themes/:

```fish
set -gx LS_COLORS (vivid generate paris-catacombes)
```

In bash or zsh:

```sh
export LS_COLORS="$(vivid generate paris-catacombes)"
```

**Paris Carrelage**, in config.fish (bash and zsh: ~/.bashrc or ~/.zshrc), after copying the theme to ~/.config/vivid/themes/:

```fish
set -gx LS_COLORS (vivid generate paris-carrelage)
```

In bash or zsh:

```sh
export LS_COLORS="$(vivid generate paris-carrelage)"
```

## Uninstall

- Delete `~/.config/vivid/themes/subway-seat.yml`.
- Delete `~/.config/vivid/themes/subway-seat-tunnel.yml`.
- Delete `~/.config/vivid/themes/subway-seat-enamel.yml`.
- Delete `~/.config/vivid/themes/london-moquette.yml`.
- Delete `~/.config/vivid/themes/london-deep-level.yml`.
- Delete `~/.config/vivid/themes/london-portland.yml`.
- Delete `~/.config/vivid/themes/paris-guimard.yml`.
- Delete `~/.config/vivid/themes/paris-catacombes.yml`.
- Delete `~/.config/vivid/themes/paris-carrelage.yml`.
- Remove the line you added to turn it on.

Generated by `build.py` from `palette.py` (v0.3.0). Edit the port in `ports/`, not these files.
