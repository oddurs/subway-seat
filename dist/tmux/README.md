# Subway Seat for tmux

A status bar like a station sign: the session as an orange route bullet (gold while the prefix is held), the active window lit in gold, a clock and a four-color stripe. The rounded bullet ends need a Nerd Font; no plugins required. With TPM, `set -g @plugin 'oddurs/subway-seat'` works too: set `@subway_seat_flavor` to walnut, tunnel, enamel or auto first, and list it before tmux-continuum.

[tmux](https://github.com/tmux/tmux) · [Previews and copy buttons](https://oddurs.github.io/subway-seat/ports/tmux/) · Needs tmux 3.2+

## Files

| Flavor | File | Where it goes |
|---|---|---|
| Subway Seat | [`subway-seat.conf`](subway-seat.conf) | `~/.config/tmux/subway-seat.conf` |
| Subway Seat Tunnel | [`subway-seat-tunnel.conf`](subway-seat-tunnel.conf) | `~/.config/tmux/subway-seat-tunnel.conf` |
| Subway Seat Enamel | [`subway-seat-enamel.conf`](subway-seat-enamel.conf) | `~/.config/tmux/subway-seat-enamel.conf` |
| London Moquette | [`london-moquette.conf`](london-moquette.conf) | `~/.config/tmux/london-moquette.conf` |
| London Deep Level | [`london-deep-level.conf`](london-deep-level.conf) | `~/.config/tmux/london-deep-level.conf` |
| London Portland | [`london-portland.conf`](london-portland.conf) | `~/.config/tmux/london-portland.conf` |

## Turn it on

**Subway Seat**, in ~/.tmux.conf or ~/.config/tmux/tmux.conf, above TPM's `run` line if you use TPM (plugins such as tmux-continuum add to the status bar when TPM runs, and this file would replace their additions):

```conf
source-file ~/.config/tmux/subway-seat.conf
```

**Subway Seat Tunnel**, in ~/.tmux.conf or ~/.config/tmux/tmux.conf, above TPM's `run` line if you use TPM (plugins such as tmux-continuum add to the status bar when TPM runs, and this file would replace their additions):

```conf
source-file ~/.config/tmux/subway-seat-tunnel.conf
```

**Subway Seat Enamel**, in ~/.tmux.conf or ~/.config/tmux/tmux.conf, above TPM's `run` line if you use TPM (plugins such as tmux-continuum add to the status bar when TPM runs, and this file would replace their additions):

```conf
source-file ~/.config/tmux/subway-seat-enamel.conf
```

**London Moquette**, in ~/.tmux.conf or ~/.config/tmux/tmux.conf, above TPM's `run` line if you use TPM (plugins such as tmux-continuum add to the status bar when TPM runs, and this file would replace their additions):

```conf
source-file ~/.config/tmux/london-moquette.conf
```

**London Deep Level**, in ~/.tmux.conf or ~/.config/tmux/tmux.conf, above TPM's `run` line if you use TPM (plugins such as tmux-continuum add to the status bar when TPM runs, and this file would replace their additions):

```conf
source-file ~/.config/tmux/london-deep-level.conf
```

**London Portland**, in ~/.tmux.conf or ~/.config/tmux/tmux.conf, above TPM's `run` line if you use TPM (plugins such as tmux-continuum add to the status bar when TPM runs, and this file would replace their additions):

```conf
source-file ~/.config/tmux/london-portland.conf
```

## Follow light and dark

In ~/.tmux.conf, in place of the line above (tmux 3.6+, in a terminal that reports light and dark changes):

```conf
source-file ~/.config/tmux/subway-seat.conf
set-hook -g client-dark-theme 'source-file ~/.config/tmux/subway-seat.conf'
set-hook -g client-light-theme 'source-file ~/.config/tmux/subway-seat-enamel.conf'
```

## Uninstall

- Delete `~/.config/tmux/subway-seat.conf`.
- Delete `~/.config/tmux/subway-seat-tunnel.conf`.
- Delete `~/.config/tmux/subway-seat-enamel.conf`.
- Delete `~/.config/tmux/london-moquette.conf`.
- Delete `~/.config/tmux/london-deep-level.conf`.
- Delete `~/.config/tmux/london-portland.conf`.
- Remove the line you added to turn it on.

Generated by `build.py` from `palette.py` (v0.3.0). Edit the port in `ports/`, not these files.
