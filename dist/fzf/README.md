# Subway Seat for fzf

Colors for the finder, preview border, pointer and marker. The files add a `--color` to the end of FZF_DEFAULT_OPTS, so your other options stay.

[fzf](https://github.com/junegunn/fzf) · [Previews and copy buttons](https://oddurs.github.io/subway-seat/ports/fzf/)

## Files

| Flavor | File | Where it goes |
|---|---|---|
| Subway Seat | [`subway-seat.fish`](subway-seat.fish) | `~/.config/fzf/subway-seat.fish` |
| Subway Seat | [`subway-seat.sh`](subway-seat.sh) | `~/.config/fzf/subway-seat.sh` |
| Subway Seat Tunnel | [`subway-seat-tunnel.fish`](subway-seat-tunnel.fish) | `~/.config/fzf/subway-seat-tunnel.fish` |
| Subway Seat Tunnel | [`subway-seat-tunnel.sh`](subway-seat-tunnel.sh) | `~/.config/fzf/subway-seat-tunnel.sh` |
| Subway Seat Enamel | [`subway-seat-enamel.fish`](subway-seat-enamel.fish) | `~/.config/fzf/subway-seat-enamel.fish` |
| Subway Seat Enamel | [`subway-seat-enamel.sh`](subway-seat-enamel.sh) | `~/.config/fzf/subway-seat-enamel.sh` |
| London Moquette | [`london-moquette.fish`](london-moquette.fish) | `~/.config/fzf/london-moquette.fish` |
| London Moquette | [`london-moquette.sh`](london-moquette.sh) | `~/.config/fzf/london-moquette.sh` |
| London Deep Level | [`london-deep-level.fish`](london-deep-level.fish) | `~/.config/fzf/london-deep-level.fish` |
| London Deep Level | [`london-deep-level.sh`](london-deep-level.sh) | `~/.config/fzf/london-deep-level.sh` |
| London Portland | [`london-portland.fish`](london-portland.fish) | `~/.config/fzf/london-portland.fish` |
| London Portland | [`london-portland.sh`](london-portland.sh) | `~/.config/fzf/london-portland.sh` |

## Turn it on

**Subway Seat**, in config.fish (bash and zsh: source the .sh file from ~/.bashrc or ~/.zshrc):

```fish
source ~/.config/fzf/subway-seat.fish
```

In bash or zsh:

```sh
. ~/.config/fzf/subway-seat.sh
```

**Subway Seat Tunnel**, in config.fish (bash and zsh: source the .sh file from ~/.bashrc or ~/.zshrc):

```fish
source ~/.config/fzf/subway-seat-tunnel.fish
```

In bash or zsh:

```sh
. ~/.config/fzf/subway-seat-tunnel.sh
```

**Subway Seat Enamel**, in config.fish (bash and zsh: source the .sh file from ~/.bashrc or ~/.zshrc):

```fish
source ~/.config/fzf/subway-seat-enamel.fish
```

In bash or zsh:

```sh
. ~/.config/fzf/subway-seat-enamel.sh
```

**London Moquette**, in config.fish (bash and zsh: source the .sh file from ~/.bashrc or ~/.zshrc):

```fish
source ~/.config/fzf/london-moquette.fish
```

In bash or zsh:

```sh
. ~/.config/fzf/london-moquette.sh
```

**London Deep Level**, in config.fish (bash and zsh: source the .sh file from ~/.bashrc or ~/.zshrc):

```fish
source ~/.config/fzf/london-deep-level.fish
```

In bash or zsh:

```sh
. ~/.config/fzf/london-deep-level.sh
```

**London Portland**, in config.fish (bash and zsh: source the .sh file from ~/.bashrc or ~/.zshrc):

```fish
source ~/.config/fzf/london-portland.fish
```

In bash or zsh:

```sh
. ~/.config/fzf/london-portland.sh
```

## Uninstall

- Delete `~/.config/fzf/subway-seat.fish`.
- Delete `~/.config/fzf/subway-seat.sh`.
- Delete `~/.config/fzf/subway-seat-tunnel.fish`.
- Delete `~/.config/fzf/subway-seat-tunnel.sh`.
- Delete `~/.config/fzf/subway-seat-enamel.fish`.
- Delete `~/.config/fzf/subway-seat-enamel.sh`.
- Delete `~/.config/fzf/london-moquette.fish`.
- Delete `~/.config/fzf/london-moquette.sh`.
- Delete `~/.config/fzf/london-deep-level.fish`.
- Delete `~/.config/fzf/london-deep-level.sh`.
- Delete `~/.config/fzf/london-portland.fish`.
- Delete `~/.config/fzf/london-portland.sh`.
- Remove the line you added to turn it on.

Generated by `build.py` from `palette.py` (v0.3.0). Edit the port in `ports/`, not these files.
