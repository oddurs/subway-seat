# Subway Seat for Atuin

Colors for the history search: gold titles, orange highlights, calm annotations, and command-line colors that match the fish port.

[Atuin](https://atuin.sh) · [Previews and copy buttons](https://oddurs.github.io/subway-seat/ports/atuin/) · Needs Atuin 18.4+

## Files

| Flavor | File | Where it goes |
|---|---|---|
| Subway Seat | [`subway-seat.toml`](subway-seat.toml) | `~/.config/atuin/themes/subway-seat.toml` |
| Subway Seat Tunnel | [`subway-seat-tunnel.toml`](subway-seat-tunnel.toml) | `~/.config/atuin/themes/subway-seat-tunnel.toml` |
| Subway Seat Enamel | [`subway-seat-enamel.toml`](subway-seat-enamel.toml) | `~/.config/atuin/themes/subway-seat-enamel.toml` |
| London Moquette | [`london-moquette.toml`](london-moquette.toml) | `~/.config/atuin/themes/london-moquette.toml` |
| London Deep Level | [`london-deep-level.toml`](london-deep-level.toml) | `~/.config/atuin/themes/london-deep-level.toml` |
| London Portland | [`london-portland.toml`](london-portland.toml) | `~/.config/atuin/themes/london-portland.toml` |

## Turn it on

**Subway Seat**, in ~/.config/atuin/config.toml:

```toml
[theme]
name = "subway-seat"
```

**Subway Seat Tunnel**, in ~/.config/atuin/config.toml:

```toml
[theme]
name = "subway-seat-tunnel"
```

**Subway Seat Enamel**, in ~/.config/atuin/config.toml:

```toml
[theme]
name = "subway-seat-enamel"
```

**London Moquette**, in ~/.config/atuin/config.toml:

```toml
[theme]
name = "london-moquette"
```

**London Deep Level**, in ~/.config/atuin/config.toml:

```toml
[theme]
name = "london-deep-level"
```

**London Portland**, in ~/.config/atuin/config.toml:

```toml
[theme]
name = "london-portland"
```

## Uninstall

- Delete `~/.config/atuin/themes/subway-seat.toml`.
- Delete `~/.config/atuin/themes/subway-seat-tunnel.toml`.
- Delete `~/.config/atuin/themes/subway-seat-enamel.toml`.
- Delete `~/.config/atuin/themes/london-moquette.toml`.
- Delete `~/.config/atuin/themes/london-deep-level.toml`.
- Delete `~/.config/atuin/themes/london-portland.toml`.
- Remove the line you added to turn it on.

Generated by `build.py` from `palette.py` (v0.3.0). Edit the port in `ports/`, not these files.
