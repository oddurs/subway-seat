# Subway Seat for kitty

Colors, cursor, selection, borders, tab bar, title bar, scrollbar and marks, plus the diff kitten's colors. `kitten themes` also lists the flavors once they're in `~/.config/kitty/themes/`. GNOME reports light mode as no-preference, so `no-preference-theme.auto.conf` is Enamel too.

[kitty](https://sw.kovidgoyal.net/kitty/) · [Previews and copy buttons](https://oddurs.github.io/subway-seat/ports/kitty/) · Needs kitty 0.43+

## Files

| Flavor | File | Where it goes |
|---|---|---|
| Subway Seat | [`subway-seat.conf`](subway-seat.conf) | `~/.config/kitty/themes/subway-seat.conf` |
| Subway Seat Tunnel | [`subway-seat-tunnel.conf`](subway-seat-tunnel.conf) | `~/.config/kitty/themes/subway-seat-tunnel.conf` |
| Subway Seat Enamel | [`subway-seat-enamel.conf`](subway-seat-enamel.conf) | `~/.config/kitty/themes/subway-seat-enamel.conf` |
| Subway Seat | [`diff/subway-seat.conf`](diff/subway-seat.conf) | `~/.config/kitty/subway-seat-diff.conf`; then add `include subway-seat-diff.conf` to ~/.config/kitty/diff.conf |
| Subway Seat Tunnel | [`diff/subway-seat-tunnel.conf`](diff/subway-seat-tunnel.conf) | `~/.config/kitty/subway-seat-tunnel-diff.conf`; then add `include subway-seat-tunnel-diff.conf` to ~/.config/kitty/diff.conf |
| Subway Seat Enamel | [`diff/subway-seat-enamel.conf`](diff/subway-seat-enamel.conf) | `~/.config/kitty/subway-seat-enamel-diff.conf`; then add `include subway-seat-enamel-diff.conf` to ~/.config/kitty/diff.conf |
| All three | [`auto/dark-theme.auto.conf`](auto/dark-theme.auto.conf) | Optional: copy to ~/.config/kitty/ to follow the OS light/dark setting |
| All three | [`auto/light-theme.auto.conf`](auto/light-theme.auto.conf) | Optional: copy to ~/.config/kitty/ to follow the OS light/dark setting |
| All three | [`auto/no-preference-theme.auto.conf`](auto/no-preference-theme.auto.conf) | Optional: copy to ~/.config/kitty/ to follow the OS light/dark setting |

## Turn it on

**Subway Seat**, in ~/.config/kitty/kitty.conf:

```conf
include themes/subway-seat.conf
```

**Subway Seat Tunnel**, in ~/.config/kitty/kitty.conf:

```conf
include themes/subway-seat-tunnel.conf
```

**Subway Seat Enamel**, in ~/.config/kitty/kitty.conf:

```conf
include themes/subway-seat-enamel.conf
```

## Follow light and dark

In a shell, in dist/kitty (kitty 0.38+; restart kitty after):

```sh
cp auto/*-theme.auto.conf ~/.config/kitty/   # Walnut for dark, Enamel for light
# Tunnel for dark instead: cp subway-seat-tunnel.conf ~/.config/kitty/dark-theme.auto.conf
```

## Uninstall

- Delete `~/.config/kitty/themes/subway-seat.conf`.
- Delete `~/.config/kitty/themes/subway-seat-tunnel.conf`.
- Delete `~/.config/kitty/themes/subway-seat-enamel.conf`.
- Delete `~/.config/kitty/subway-seat-diff.conf`.
- Delete `~/.config/kitty/subway-seat-tunnel-diff.conf`.
- Delete `~/.config/kitty/subway-seat-enamel-diff.conf`.
- Remove the line you added to turn it on.

Generated by `build.py` from `palette.py` (v0.3.0). Edit the port in `ports/`, not these files.
