# Subway Seat for Helix

Copy the themes to `~/.config/helix/themes/` (Windows: `%AppData%\helix\themes`). The colored mode badge in the status line needs `color-modes = true` under `[editor]`. Rainbow brackets come from the theme's `rainbow` key, which Helix 25.07.1 skips with a warning in its log; builds after it read it.

[Helix](https://helix-editor.com) · [Previews and copy buttons](https://oddurs.github.io/subway-seat/ports/helix/)

## Files

| Flavor | File | Where it goes |
|---|---|---|
| Subway Seat | [`subway_seat.toml`](subway_seat.toml) | `~/.config/helix/themes/subway_seat.toml` |
| Subway Seat Tunnel | [`subway_seat_tunnel.toml`](subway_seat_tunnel.toml) | `~/.config/helix/themes/subway_seat_tunnel.toml` |
| Subway Seat Enamel | [`subway_seat_enamel.toml`](subway_seat_enamel.toml) | `~/.config/helix/themes/subway_seat_enamel.toml` |
| London Moquette | [`london_moquette.toml`](london_moquette.toml) | `~/.config/helix/themes/london_moquette.toml` |
| London Deep Level | [`london_deep_level.toml`](london_deep_level.toml) | `~/.config/helix/themes/london_deep_level.toml` |
| London Portland | [`london_portland.toml`](london_portland.toml) | `~/.config/helix/themes/london_portland.toml` |

## Turn it on

**Subway Seat**, in ~/.config/helix/config.toml (Windows: %AppData%\helix\config.toml):

```toml
theme = "subway_seat"
```

**Subway Seat Tunnel**, in ~/.config/helix/config.toml (Windows: %AppData%\helix\config.toml):

```toml
theme = "subway_seat_tunnel"
```

**Subway Seat Enamel**, in ~/.config/helix/config.toml (Windows: %AppData%\helix\config.toml):

```toml
theme = "subway_seat_enamel"
```

**London Moquette**, in ~/.config/helix/config.toml (Windows: %AppData%\helix\config.toml):

```toml
theme = "london_moquette"
```

**London Deep Level**, in ~/.config/helix/config.toml (Windows: %AppData%\helix\config.toml):

```toml
theme = "london_deep_level"
```

**London Portland**, in ~/.config/helix/config.toml (Windows: %AppData%\helix\config.toml):

```toml
theme = "london_portland"
```

## Follow light and dark

In ~/.config/helix/config.toml, in place of the `theme = …` line. Needs a Helix build newer than 25.07.1 (25.07.1 rejects a `[theme]` table) and a terminal that reports light or dark (mode 2031):

```toml
[theme]
dark = "subway_seat"
light = "subway_seat_enamel"
fallback = "subway_seat"
```

## Uninstall

- Delete `~/.config/helix/themes/subway_seat.toml`.
- Delete `~/.config/helix/themes/subway_seat_tunnel.toml`.
- Delete `~/.config/helix/themes/subway_seat_enamel.toml`.
- Delete `~/.config/helix/themes/london_moquette.toml`.
- Delete `~/.config/helix/themes/london_deep_level.toml`.
- Delete `~/.config/helix/themes/london_portland.toml`.
- Remove the line you added to turn it on.

Generated by `build.py` from `palette.py` (v0.3.0). Edit the port in `ports/`, not these files.
