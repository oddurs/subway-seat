# Subway Seat for gh-dash

Parchment text, orange borders on the active section and a warm selected row, with author-role icons in the palette's accents. gh-dash shows PR diffs through its pager, so set `pager: {diff: delta}` in the same config to see them with the delta port.

[gh-dash](https://github.com/dlvhdr/gh-dash) · [Previews and copy buttons](https://oddurs.github.io/subway-seat/ports/gh-dash/) · Needs gh-dash 4.25+ for `include`

## The quick way

```sh
curl -fsSL https://oddurs.github.io/subway-seat/install.sh | sh -s -- --only gh-dash
```

The [installer](../../docs/INSTALL.md) shows its plan and asks once. Add `--flavor tunnel`, `--flavor enamel` or `--flavor auto` for another flavor. To do it yourself, use the files below.

## Files

| Flavor | File | Where it goes |
|---|---|---|
| Subway Seat | [`subway-seat.yml`](subway-seat.yml) | `~/.config/gh-dash/subway-seat.yml` |
| Subway Seat Tunnel | [`subway-seat-tunnel.yml`](subway-seat-tunnel.yml) | `~/.config/gh-dash/subway-seat-tunnel.yml` |
| Subway Seat Enamel | [`subway-seat-enamel.yml`](subway-seat-enamel.yml) | `~/.config/gh-dash/subway-seat-enamel.yml` |
| London Moquette | [`london-moquette.yml`](london-moquette.yml) | `~/.config/gh-dash/london-moquette.yml` |
| London Deep Level | [`london-deep-level.yml`](london-deep-level.yml) | `~/.config/gh-dash/london-deep-level.yml` |
| London Portland | [`london-portland.yml`](london-portland.yml) | `~/.config/gh-dash/london-portland.yml` |
| Paris Guimard | [`paris-guimard.yml`](paris-guimard.yml) | `~/.config/gh-dash/paris-guimard.yml` |
| Paris Catacombes | [`paris-catacombes.yml`](paris-catacombes.yml) | `~/.config/gh-dash/paris-catacombes.yml` |
| Paris Carrelage | [`paris-carrelage.yml`](paris-carrelage.yml) | `~/.config/gh-dash/paris-carrelage.yml` |

## Turn it on

**Subway Seat**, in ~/.config/gh-dash/config.yml (on releases before 4.25, paste the theme block in instead):

```yaml
include:
  - ~/.config/gh-dash/subway-seat.yml
```

**Subway Seat Tunnel**, in ~/.config/gh-dash/config.yml (on releases before 4.25, paste the theme block in instead):

```yaml
include:
  - ~/.config/gh-dash/subway-seat-tunnel.yml
```

**Subway Seat Enamel**, in ~/.config/gh-dash/config.yml (on releases before 4.25, paste the theme block in instead):

```yaml
include:
  - ~/.config/gh-dash/subway-seat-enamel.yml
```

**London Moquette**, in ~/.config/gh-dash/config.yml (on releases before 4.25, paste the theme block in instead):

```yaml
include:
  - ~/.config/gh-dash/london-moquette.yml
```

**London Deep Level**, in ~/.config/gh-dash/config.yml (on releases before 4.25, paste the theme block in instead):

```yaml
include:
  - ~/.config/gh-dash/london-deep-level.yml
```

**London Portland**, in ~/.config/gh-dash/config.yml (on releases before 4.25, paste the theme block in instead):

```yaml
include:
  - ~/.config/gh-dash/london-portland.yml
```

**Paris Guimard**, in ~/.config/gh-dash/config.yml (on releases before 4.25, paste the theme block in instead):

```yaml
include:
  - ~/.config/gh-dash/paris-guimard.yml
```

**Paris Catacombes**, in ~/.config/gh-dash/config.yml (on releases before 4.25, paste the theme block in instead):

```yaml
include:
  - ~/.config/gh-dash/paris-catacombes.yml
```

**Paris Carrelage**, in ~/.config/gh-dash/config.yml (on releases before 4.25, paste the theme block in instead):

```yaml
include:
  - ~/.config/gh-dash/paris-carrelage.yml
```

## Uninstall

- Delete `~/.config/gh-dash/subway-seat.yml`.
- Delete `~/.config/gh-dash/subway-seat-tunnel.yml`.
- Delete `~/.config/gh-dash/subway-seat-enamel.yml`.
- Delete `~/.config/gh-dash/london-moquette.yml`.
- Delete `~/.config/gh-dash/london-deep-level.yml`.
- Delete `~/.config/gh-dash/london-portland.yml`.
- Delete `~/.config/gh-dash/paris-guimard.yml`.
- Delete `~/.config/gh-dash/paris-catacombes.yml`.
- Delete `~/.config/gh-dash/paris-carrelage.yml`.
- Remove the line you added to turn it on.

Generated by `build.py` from `palette.py` (v0.3.0). Edit the port in `ports/`, not these files.
