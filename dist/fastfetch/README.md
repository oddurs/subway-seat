# Subway Seat for fastfetch

Gold keys, an orange title, quiet separators, and percentage bars and temperatures that go avocado, gold, redbird. Logo colors are repainted along the 70s stripe, so the Apple logo comes out in avocado, gold, orange, redbird, terracotta and denim. The file lists fastfetch's default modules; to keep your own, copy its logo and display blocks into config.jsonc instead.

[fastfetch](https://github.com/fastfetch-cli/fastfetch) · [Previews and copy buttons](https://oddurs.github.io/subway-seat/ports/fastfetch/) · Needs fastfetch 2.14+

## The quick way

```sh
curl -fsSL https://oddurs.github.io/subway-seat/install.sh | sh -s -- --only fastfetch
```

The [installer](../../docs/INSTALL.md) shows its plan and asks once. Add `--flavor tunnel`, `--flavor enamel` or `--flavor auto` for another flavor. To do it yourself, use the files below.

## Files

| Flavor | File | Where it goes |
|---|---|---|
| Subway Seat | [`subway-seat.jsonc`](subway-seat.jsonc) | `~/.config/fastfetch/subway-seat.jsonc` |
| Subway Seat Tunnel | [`subway-seat-tunnel.jsonc`](subway-seat-tunnel.jsonc) | `~/.config/fastfetch/subway-seat-tunnel.jsonc` |
| Subway Seat Enamel | [`subway-seat-enamel.jsonc`](subway-seat-enamel.jsonc) | `~/.config/fastfetch/subway-seat-enamel.jsonc` |
| London Moquette | [`london-moquette.jsonc`](london-moquette.jsonc) | `~/.config/fastfetch/london-moquette.jsonc` |
| London Deep Level | [`london-deep-level.jsonc`](london-deep-level.jsonc) | `~/.config/fastfetch/london-deep-level.jsonc` |
| London Portland | [`london-portland.jsonc`](london-portland.jsonc) | `~/.config/fastfetch/london-portland.jsonc` |
| Paris Guimard | [`paris-guimard.jsonc`](paris-guimard.jsonc) | `~/.config/fastfetch/paris-guimard.jsonc` |
| Paris Catacombes | [`paris-catacombes.jsonc`](paris-catacombes.jsonc) | `~/.config/fastfetch/paris-catacombes.jsonc` |
| Paris Carrelage | [`paris-carrelage.jsonc`](paris-carrelage.jsonc) | `~/.config/fastfetch/paris-carrelage.jsonc` |

## Turn it on

**Subway Seat**, in your shell or an alias:

```sh
fastfetch --config subway-seat
```

**Subway Seat Tunnel**, in your shell or an alias:

```sh
fastfetch --config subway-seat-tunnel
```

**Subway Seat Enamel**, in your shell or an alias:

```sh
fastfetch --config subway-seat-enamel
```

**London Moquette**, in your shell or an alias:

```sh
fastfetch --config london-moquette
```

**London Deep Level**, in your shell or an alias:

```sh
fastfetch --config london-deep-level
```

**London Portland**, in your shell or an alias:

```sh
fastfetch --config london-portland
```

**Paris Guimard**, in your shell or an alias:

```sh
fastfetch --config paris-guimard
```

**Paris Catacombes**, in your shell or an alias:

```sh
fastfetch --config paris-catacombes
```

**Paris Carrelage**, in your shell or an alias:

```sh
fastfetch --config paris-carrelage
```

## Uninstall

- Delete `~/.config/fastfetch/subway-seat.jsonc`.
- Delete `~/.config/fastfetch/subway-seat-tunnel.jsonc`.
- Delete `~/.config/fastfetch/subway-seat-enamel.jsonc`.
- Delete `~/.config/fastfetch/london-moquette.jsonc`.
- Delete `~/.config/fastfetch/london-deep-level.jsonc`.
- Delete `~/.config/fastfetch/london-portland.jsonc`.
- Delete `~/.config/fastfetch/paris-guimard.jsonc`.
- Delete `~/.config/fastfetch/paris-catacombes.jsonc`.
- Delete `~/.config/fastfetch/paris-carrelage.jsonc`.
- Remove the line you added to turn it on.

Generated by `build.py` from `palette.py` (v0.3.0). Edit the port in `ports/`, not these files.
